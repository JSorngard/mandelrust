[package]
name = "color-space"
version = "0.3.1"
edition = "2021"

[dependencies]
image = {version = "0.24", default-features = false, features = ["png"]}

[dev-dependencies]
approx = "0.5"
itertools = "0.10"
